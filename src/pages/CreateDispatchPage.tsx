const CreateDispatchPage = () => {
    return (
        <div className='flex items-center justify-center h-full text-black/50'>
            Create Dispatch — coming soon
        </div>
    );
};

export default CreateDispatchPage;
